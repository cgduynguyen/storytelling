/**
 * Expo Router Entry Point
 *
 * This file is kept for compatibility with the "main" field in package.json.
 * Expo Router's entry point is configured in package.json as "expo-router/entry".
 * All providers and app initialization are handled in src/app/_layout.tsx.
 */
import 'expo-router/entry';
